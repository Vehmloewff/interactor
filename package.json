{
	"name": "interactor",
	"version": "0.1.3",
	"description": "CLI for controlling and inspecting live browser sessions from scripts, terminals, and agent workflows",
	"type": "module",
	"license": "MIT",
	"scripts": {
		"start": "bun run src/main.ts",
		"check": "bunx tsc --noEmit",
		"now": "bun run src/scripts/now.ts"
	},
	"dependencies": {
		"commander": "^14.0.3",
		"playwright": "^1.58.2",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@types/bun": "^1.3.9",
		"typescript": "^5.9.3"
	}
}
